export interface IUserDto {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
  }
  
  export interface IUserUpdateDto {
    firstName: string;
    lastName: string;
  }
